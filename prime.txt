#!/bin/bash

echo "Enter a number:"
read num
isPrime=1  # assume number is prime

for (( i=2; i<num; i++ ))
do
    if (( num % i == 0 ))
    then
        isPrime=0
        break
    fi
done

if (( isPrime == 1 ))
then
    echo "$num is Prime"
else
    echo "$num is Not Prime"
fi
